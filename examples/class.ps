class Program {
    priv let int field;

    pub (f: int) -> field(f) {          // Constructor
        // statements
    }

    pub fun bool ==(other: Program) {   // Operator '==' overload
        // statements
    }

    pub fun int main() {                // Method ('main' is entry point)
        // statements
    }
}